/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',  // Ensure that Next.js exports static HTML
    distDir: 'out',  // Output directory
  };
  
  export default nextConfig;
  